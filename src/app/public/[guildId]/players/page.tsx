import Link from "next/link"
import { ArrowUpRight, Search } from "lucide-react"
import { PublicCard, SectionHeading } from "../public-shell"
import { publicPlayers } from "../data"

export default async function PlayersPage({ params }: { params: Promise<{ guildId: string }> }) { const { guildId } = await params; const base = `/public/${guildId}`; return <div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><SectionHeading eyebrow="PLAYER INDEX" title="Meet the competitors." description="Every match adds to the story. Explore the players climbing through Season 14." /><PublicCard className="overflow-hidden"><div className="flex items-center gap-3 border-b border-white/[0.08] p-4"><Search className="size-4 text-white/35" /><input aria-label="Search players" placeholder="Search players..." className="w-full bg-transparent text-sm text-white outline-none placeholder:text-white/30" /></div><div className="grid gap-3 p-4 sm:grid-cols-2 lg:grid-cols-3">{publicPlayers.map((player, index) => <Link key={player.id} href={`${base}/players/${player.id}`} className="group rounded-xl border border-white/[0.07] bg-black/10 p-4 transition-all hover:-translate-y-0.5 hover:border-cyan-300/35 hover:bg-cyan-300/[0.04]"><div className="flex items-center gap-3"><div className="grid size-11 place-items-center rounded-full bg-gradient-to-br from-cyan-300/30 to-violet-400/30 text-sm font-black text-cyan-100">{player.avatar}</div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><h3 className="truncate text-sm font-bold">{player.name}</h3><ArrowUpRight className="size-4 text-white/25 transition-transform group-hover:-translate-y-0.5 group-hover:translate-x-0.5" /></div><p className="mt-1 font-mono text-[10px] tracking-wider text-cyan-300">{player.rank}</p></div></div><div className="mt-5 grid grid-cols-3 gap-2 border-t border-white/[0.07] pt-3 text-center"><div><p className="font-mono text-[10px] text-white/35">ELO</p><p className="mt-1 text-sm font-bold">{player.elo}</p></div><div><p className="font-mono text-[10px] text-white/35">WINS</p><p className="mt-1 text-sm font-bold">{player.wins}</p></div><div><p className="font-mono text-[10px] text-white/35">STREAK</p><p className="mt-1 text-sm font-bold text-emerald-300">{player.streak}W</p></div></div><span className="mt-4 block font-mono text-[10px] text-white/30">RANK #{index + 1}</span></Link>)}</div></PublicCard></div> }
