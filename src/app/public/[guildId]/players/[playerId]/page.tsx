import Link from "next/link"
import { ArrowLeft, Flame, Swords, Trophy } from "lucide-react"
import type { ComponentType } from "react"
import { PublicCard } from "../../public-shell"
import { publicPlayers } from "../../data"

export default async function PlayerPage({ params }: { params: Promise<{ guildId: string; playerId: string }> }) { const { guildId, playerId } = await params; const player = publicPlayers.find((entry) => entry.id === playerId) ?? publicPlayers[0]; const stats: Array<[string, string | number, ComponentType<{ className?: string }>]> = [["ELO", player.elo, Trophy], ["WINS", player.wins, Swords], ["GAMES", player.games, Swords], ["STREAK", `${player.streak}W`, Flame]]; return <div className="mx-auto max-w-5xl px-5 py-16 sm:px-8 sm:py-24"><Link href={`/public/${guildId}/players`} className="mb-10 inline-flex items-center gap-2 text-xs font-bold text-white/45 transition-colors hover:text-cyan-300"><ArrowLeft className="size-3.5" /> Back to players</Link><PublicCard className="overflow-hidden"><div className="h-32 bg-gradient-to-r from-cyan-400/25 via-violet-400/15 to-transparent" /><div className="-mt-12 p-6 sm:p-8"><div className="flex flex-col gap-5 sm:flex-row sm:items-end"><div className="grid size-24 place-items-center rounded-2xl border-4 border-[#070a0f] bg-gradient-to-br from-cyan-300/40 to-violet-400/40 text-3xl font-black">{player.avatar}</div><div><p className="font-mono text-[10px] tracking-[.2em] text-cyan-300">PLAYER PROFILE</p><h1 className="mt-2 text-4xl font-black">{player.name}</h1><p className="mt-1 text-sm text-white/45">{player.rank} division · Season 14</p></div></div><div className="mt-10 grid gap-3 sm:grid-cols-4">{stats.map(([label, value, Icon]) => <div key={label} className="rounded-xl bg-black/20 p-4"><Icon className="size-4 text-cyan-300" /><p className="mt-4 font-mono text-[10px] text-white/35">{label}</p><p className="mt-1 text-xl font-black">{value}</p></div>)}</div></div></PublicCard></div> }
