import { Gamepad2 } from "lucide-react"
import { PublicCard, SectionHeading } from "../public-shell"
import { publicGames } from "../data"

export default async function GamesPage({ params }: { params: Promise<{ guildId: string }> }) { await params; return <div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><SectionHeading eyebrow="MATCH HISTORY" title="Games worth replaying." description="A live feed of the latest matches across every queue and map." /><PublicCard className="overflow-hidden"><div className="grid grid-cols-[1fr_auto_auto] gap-4 border-b border-white/[0.08] px-5 py-4 font-mono text-[10px] tracking-wider text-white/35"><span>MATCH</span><span>RESULT</span><span>WHEN</span></div><div className="divide-y divide-white/[0.07]">{publicGames.map(game => <div key={game.id} className="grid grid-cols-[1fr_auto_auto] items-center gap-4 px-5 py-5"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-lg bg-cyan-300/10 text-cyan-300"><Gamepad2 className="size-4" /></span><div><p className="font-mono text-xs text-white/40">{game.id} · {game.mode}</p><p className="mt-1 text-sm font-bold">{game.map}</p></div></div><div className="text-right"><p className="text-sm font-bold text-emerald-300">{game.winner}</p><p className="mt-1 font-mono text-xs text-white/45">{game.score}</p></div><span className="text-right font-mono text-[11px] text-white/35">{game.time}</span></div>)}</div></PublicCard></div> }
