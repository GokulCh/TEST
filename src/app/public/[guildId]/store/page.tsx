import { Check, ShoppingBag } from "lucide-react"
import { PublicCard, SectionHeading } from "../public-shell"
import { storeItems } from "../data"

export default async function StorePage({ params }: { params: Promise<{ guildId: string }> }) { await params; return <div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><SectionHeading eyebrow="COMMUNITY STORE" title="Back the network." description="Optional supporter perks that keep the servers fast and the competition fair." /><div className="grid gap-4 md:grid-cols-3">{storeItems.map(item => <PublicCard key={item.name} className={`bg-gradient-to-br ${item.accent} to-transparent p-6`}><div className="flex items-center justify-between"><span className="grid size-10 place-items-center rounded-xl bg-white/[0.08] text-cyan-300"><ShoppingBag className="size-5" /></span><span className="font-mono text-lg font-bold">{item.price}</span></div><h2 className="mt-10 text-xl font-black">{item.name}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-white/50">{item.detail}</p><button className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl border border-white/15 px-4 py-3 text-xs font-bold transition-colors hover:bg-white/[0.08]">Get this perk <Check className="size-3.5" /></button></PublicCard>)}</div><p className="mt-8 text-center font-mono text-[10px] tracking-wider text-white/30">Payments are handled securely. Store items never affect competitive matchmaking.</p></div> }
