"use client"

import { Crown } from "lucide-react"
import { useMemo, useState } from "react"
import { PublicCard, SectionHeading } from "../public-shell"
import { publicPlayers } from "../data"
import { PublicSelect } from "../public-filters"

export default function LeaderboardPage() { const [rank, setRank] = useState("ALL RANKS"); const [sort, setSort] = useState("ELO"); const [query, setQuery] = useState(""); const players = useMemo(() => publicPlayers.filter((player) => player.name.toLowerCase().includes(query.toLowerCase()) && (rank === "ALL RANKS" || player.rank === rank)).sort((a, b) => sort === "WINS" ? b.wins - a.wins : sort === "STREAK" ? b.streak - a.streak : b.elo - a.elo), [query, rank, sort]); return <div className="mx-auto max-w-[95rem] px-5 py-16 sm:px-8 sm:py-24"><SectionHeading eyebrow="SEASON 14 RANKINGS" title="The climb starts here." description="Ranked by ELO across the active competitive season." /><div className="mb-5 flex flex-wrap gap-3"><input aria-label="Search leaderboard" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search leaderboard..." className="min-w-56 flex-1 rounded-xl border border-white/[0.09] bg-white/[0.025] px-4 py-3 text-sm text-white outline-none placeholder:text-white/30 focus:border-cyan-300/40" /><PublicSelect label="RANK" value={rank} onChange={setRank} options={["ALL RANKS", "MASTER", "DIAMOND", "PLATINUM", "GOLD"]} /><PublicSelect label="SORT" value={sort} onChange={setSort} options={["ELO", "WINS", "STREAK"]} /></div><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr] lg:items-start"><PublicCard className="bg-gradient-to-br from-violet-400/15 via-white/[0.025] to-cyan-300/10 p-6"><Crown className="size-7 text-amber-300" /><p className="mt-8 font-mono text-[10px] tracking-[.2em] text-white/40">CURRENT LEADER</p><h2 className="mt-2 text-3xl font-black">{players[0]?.name ?? "—"}</h2><p className="mt-1 text-sm text-cyan-300">{players[0] ? `${players[0].rank} · ${players[0].elo} ELO` : "No matching players"}</p><div className="mt-8 grid grid-cols-2 gap-3"><div className="rounded-xl bg-black/20 p-3"><p className="font-mono text-[10px] text-white/35">WIN RATE</p><p className="mt-1 text-lg font-bold">72.4%</p></div><div className="rounded-xl bg-black/20 p-3"><p className="font-mono text-[10px] text-white/35">STREAK</p><p className="mt-1 text-lg font-bold text-emerald-300">{players[0]?.streak ?? 0} wins</p></div></div></PublicCard><PublicCard className="overflow-hidden"><div className="grid grid-cols-[44px_1fr_auto] gap-4 border-b border-white/[0.08] px-5 py-4 font-mono text-[10px] tracking-wider text-white/35"><span>#</span><span>PLAYER</span><span>ELO</span></div><div className="divide-y divide-white/[0.07]">{players.map((player, index) => <div key={player.id} className="grid grid-cols-[44px_1fr_auto] items-center gap-4 px-5 py-4"><span className={`font-mono text-sm ${index < 3 ? "text-amber-300" : "text-white/35"}`}>{String(index + 1).padStart(2, "0")}</span><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-white/[0.08] text-xs font-bold">{player.avatar}</span><div><p className="text-sm font-bold">{player.name}</p><p className="font-mono text-[10px] text-white/40">{player.rank} · {player.wins} wins</p></div></div><span className="font-mono text-sm font-bold text-cyan-300">{player.elo}</span></div>)}</div>{players.length === 0 && <p className="p-10 text-center text-sm text-white/45">No players match those filters.</p>}</PublicCard></div></div> }
