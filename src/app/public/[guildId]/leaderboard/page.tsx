import { Crown, Medal } from "lucide-react"
import { PublicCard, SectionHeading } from "../public-shell"
import { publicPlayers } from "../data"

export default async function LeaderboardPage({ params }: { params: Promise<{ guildId: string }> }) { await params; return <div className="mx-auto max-w-7xl px-5 py-16 sm:px-8 sm:py-24"><SectionHeading eyebrow="SEASON 14 RANKINGS" title="The climb starts here." description="Ranked by ELO across the active competitive season." /><div className="grid gap-5 lg:grid-cols-[.8fr_1.2fr] lg:items-start"><PublicCard className="bg-gradient-to-br from-violet-400/15 via-white/[0.025] to-cyan-300/10 p-6"><Crown className="size-7 text-amber-300" /><p className="mt-8 font-mono text-[10px] tracking-[.2em] text-white/40">CURRENT LEADER</p><h2 className="mt-2 text-3xl font-black">{publicPlayers[0].name}</h2><p className="mt-1 text-sm text-cyan-300">{publicPlayers[0].rank} · {publicPlayers[0].elo} ELO</p><div className="mt-8 grid grid-cols-2 gap-3"><div className="rounded-xl bg-black/20 p-3"><p className="font-mono text-[10px] text-white/35">WIN RATE</p><p className="mt-1 text-lg font-bold">72.4%</p></div><div className="rounded-xl bg-black/20 p-3"><p className="font-mono text-[10px] text-white/35">STREAK</p><p className="mt-1 text-lg font-bold text-emerald-300">8 wins</p></div></div></PublicCard><PublicCard className="overflow-hidden"><div className="grid grid-cols-[44px_1fr_auto] gap-4 border-b border-white/[0.08] px-5 py-4 font-mono text-[10px] tracking-wider text-white/35"><span>#</span><span>PLAYER</span><span>ELO</span></div><div className="divide-y divide-white/[0.07]">{publicPlayers.map((player, index) => <div key={player.id} className="grid grid-cols-[44px_1fr_auto] items-center gap-4 px-5 py-4"><span className={`font-mono text-sm ${index < 3 ? "text-amber-300" : "text-white/35"}`}>{String(index + 1).padStart(2, "0")}</span><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-full bg-white/[0.08] text-xs font-bold">{player.avatar}</span><div><p className="text-sm font-bold">{player.name}</p><p className="font-mono text-[10px] text-white/35">{player.rank}</p></div></div><span className="font-mono text-sm font-bold">{player.elo}</span></div>)}</div></PublicCard></div></div> }
