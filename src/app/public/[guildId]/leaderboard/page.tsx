"use client"

import { Crown, Search, TrendingUp } from "lucide-react"
import { useMemo, useState } from "react"
import { PublicCard, SectionHeading } from "../public-shell"
import { publicPlayers } from "../data"
import { PublicSelect } from "../public-filters"

export default function LeaderboardPage() {
  const [rank, setRank] = useState("ALL RANKS")
  const [sort, setSort] = useState("ELO")
  const [query, setQuery] = useState("")
  const players = useMemo(() => publicPlayers.filter((player) => player.name.toLowerCase().includes(query.toLowerCase()) && (rank === "ALL RANKS" || player.rank === rank)).sort((a, b) => sort === "WINS" ? b.wins - a.wins : sort === "STREAK" ? b.streak - a.streak : b.elo - a.elo), [query, rank, sort])
  const podium = players.slice(0, 3)
  return <div className="mx-auto max-w-[95rem] px-5 py-16 sm:px-8 sm:py-24"><SectionHeading eyebrow="SEASON 14 RANKINGS" title="The climb starts here." description="Every win moves the board. Follow the active season and see who is setting the pace." /><div className="mb-8 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border border-white/[0.08] bg-white/[0.025] p-4"><p className="font-mono text-[10px] text-white/35">PLAYERS RANKED</p><p className="mt-2 text-2xl font-black">{players.length}</p></div><div className="rounded-xl border border-white/[0.08] bg-white/[0.025] p-4"><p className="font-mono text-[10px] text-white/35">HIGHEST ELO</p><p className="mt-2 text-2xl font-black">{players[0]?.elo ?? 0}</p></div><div className="rounded-xl border border-white/[0.08] bg-white/[0.025] p-4"><p className="font-mono text-[10px] text-white/35">SEASON STATUS</p><p className="mt-2 flex items-center gap-2 text-sm font-bold text-emerald-300"><i className="size-1.5 rounded-full bg-emerald-400" /> ACTIVE</p></div></div><div className="mb-5 flex flex-wrap gap-3"><label className="relative min-w-56 flex-1"><Search className="absolute left-4 top-1/2 size-4 -translate-y-1/2 text-white/30" /><input aria-label="Search leaderboard" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search a player..." className="w-full rounded-xl border border-white/[0.09] bg-white/[0.025] py-3 pl-11 pr-4 text-sm text-white outline-none placeholder:text-white/30 focus:border-cyan-300/40" /></label><PublicSelect label="RANK" value={rank} onChange={setRank} options={["ALL RANKS", "MASTER", "DIAMOND", "PLATINUM", "GOLD"]} /><PublicSelect label="SORT" value={sort} onChange={setSort} options={["ELO", "WINS", "STREAK"]} /></div><div className="mb-8 grid gap-4 md:grid-cols-3 md:items-end">{podium.map((player, index) => <PublicCard key={player.id} className={`relative p-5 ${index === 0 ? "md:-translate-y-4 border-amber-300/30 bg-amber-300/[0.06]" : ""}`}><div className="flex items-center justify-between"><span className="grid size-9 place-items-center rounded-full border border-white/15 bg-white/[0.05] text-sm font-black">{index + 1}</span>{index === 0 && <Crown className="size-5 text-amber-300" />}</div><p className="mt-8 text-xl font-black">{player.name}</p><p className="mt-1 font-mono text-[10px] tracking-[.14em] text-cyan-300">{player.rank}</p><div className="mt-5 flex items-end justify-between"><span className="text-3xl font-black">{player.elo}</span><span className="font-mono text-[10px] text-white/35">ELO</span></div><div className="mt-4 h-1 rounded-full bg-white/[0.08]"><div className={`h-full rounded-full ${index === 0 ? "bg-amber-300" : "bg-cyan-300"}`} style={{ width: `${Math.min(100, player.elo / 20)}%` }} /></div></PublicCard>)}</div><PublicCard className="overflow-hidden"><div className="grid grid-cols-[3rem_1fr_7rem_6rem] gap-3 border-b border-white/[0.08] px-5 py-4 font-mono text-[10px] tracking-[.15em] text-white/35"><span>#</span><span>PLAYER</span><span>RANK</span><span className="text-right">ELO</span></div><div className="divide-y divide-white/[0.07]">{players.map((player, index) => <div key={player.id} className="grid grid-cols-[3rem_1fr_7rem_6rem] items-center gap-3 px-5 py-4 transition-colors hover:bg-white/[0.04]"><span className="font-mono text-sm text-white/35">{index + 1}</span><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-lg bg-cyan-300/10 text-xs font-black text-cyan-200">{player.avatar}</span><div><p className="text-sm font-bold">{player.name}</p><p className="font-mono text-[10px] text-white/30">{player.wins} wins · {player.streak} streak</p></div></div><span className="rounded-full border border-white/10 px-2 py-1 text-center font-mono text-[10px] text-white/60">{player.rank}</span><span className="text-right text-sm font-black">{player.elo}</span></div>)}{players.length === 0 && <div className="p-12 text-center font-mono text-xs uppercase tracking-widest text-white/35">No ranked players match your search.</div>}</div></PublicCard><div className="mt-5 flex items-center gap-2 font-mono text-[10px] text-white/35"><TrendingUp className="size-3.5 text-cyan-300" /> Rankings update as new match results are processed.</div></div>
}
