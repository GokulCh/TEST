"use client"

import Link from "next/link"
import { ArrowLeft, GripVertical, Pencil, Plus, RotateCcw, Search, Trash2 } from "lucide-react"
import { use, useMemo, useState } from "react"
import { publicPlayers } from "../data"
import { PublicCard, SectionHeading } from "../public-shell"

type Tier = { id: string; label: string; color: string; players: string[] }
const palette = ["#ff4d5b", "#ff8246", "#ffad45", "#ffd447", "#75df82", "#4bbcf3", "#a77cff", "#e78cff"]
const initialTiers: Tier[] = ["S+", "S", "A+", "A", "B", "C", "D", "F"].map((label, index) => ({ id: `${label}-${index}`, label, color: palette[index], players: [] }))

export default function TierListsPage({ params }: { params: Promise<{ guildId: string }> }) {
  const { guildId } = use(params)
  const [tiers, setTiers] = useState(initialTiers)
  const [search, setSearch] = useState("")
  const [selected, setSelected] = useState<string | null>(null)
  const assigned = new Set(tiers.flatMap((tier) => tier.players))
  const pool = useMemo(() => publicPlayers.filter((player) => !assigned.has(player.id) && player.name.toLowerCase().includes(search.toLowerCase())), [assigned, search])
  const updateTier = (id: string, patch: Partial<Tier>) => setTiers((current) => current.map((tier) => tier.id === id ? { ...tier, ...patch } : tier))
  const place = (tierId: string, playerId: string) => { setTiers((current) => current.map((tier) => ({ ...tier, players: tier.id === tierId ? [...tier.players, playerId] : tier.players.filter((id) => id !== playerId) }))); setSelected(null) }
  const removeTier = (id: string) => setTiers((current) => current.length > 1 ? current.filter((tier) => tier.id !== id) : current)
  const reset = () => { setTiers(initialTiers); setSelected(null) }

  return <main className="mx-auto max-w-[95rem] px-5 py-12 sm:px-8 sm:py-20"><Link href={`/public/${guildId}`} className="mb-10 inline-flex items-center gap-2 rounded-lg border border-white/10 px-3 py-2 text-xs font-semibold text-white/55 hover:border-cyan-300/30 hover:text-cyan-200"><ArrowLeft className="size-3.5" /> Community hub</Link><div className="mx-auto max-w-4xl text-center"><SectionHeading eyebrow="TIER LIST MAKER" title="Rank the players." description="Drag players into a tier, or select one from the pool and tap a row. Rename, recolor, add, and remove tiers to make the board yours." /></div><div className="mx-auto mt-10 max-w-6xl space-y-2">{tiers.map((tier) => <div key={tier.id} className="flex min-h-16 overflow-hidden rounded-xl border border-white/10 bg-white/[0.025] transition hover:border-white/20" onDragOver={(event) => event.preventDefault()} onDrop={(event) => { const id = event.dataTransfer.getData("playerId"); if (id) place(tier.id, id) }}><div className="flex w-20 shrink-0 items-center justify-center sm:w-28" style={{ backgroundColor: tier.color }}><input aria-label={`Rename ${tier.label} tier`} value={tier.label} onChange={(event) => updateTier(tier.id, { label: event.target.value })} className="w-full bg-transparent px-2 text-center text-xl font-black text-[#071016] outline-none sm:text-2xl" /></div><div className="flex min-w-0 flex-1 flex-wrap items-center gap-2 p-2" onClick={() => selected && place(tier.id, selected)}>{tier.players.map((playerId) => { const player = publicPlayers.find((item) => item.id === playerId); return player ? <button key={player.id} type="button" onClick={() => setTiers((current) => current.map((item) => ({ ...item, players: item.players.filter((id) => id !== player.id) })))} className="flex items-center gap-2 rounded-lg border border-white/10 bg-black/25 px-3 py-2 text-xs font-semibold text-white/80"><span className="grid size-6 place-items-center rounded-md bg-cyan-400/15 font-mono text-[10px] text-cyan-200">{player.avatar}</span>{player.name}</button> : null })}{tier.players.length === 0 && <span className="px-2 font-mono text-[10px] uppercase tracking-widest text-white/25">Drop players here</span>}</div><div className="flex items-center gap-1 px-2"><input aria-label={`Color ${tier.label} tier`} type="color" value={tier.color} onChange={(event) => updateTier(tier.id, { color: event.target.value })} className="size-7 cursor-pointer rounded bg-transparent" /><button type="button" aria-label={`Delete ${tier.label} tier`} onClick={() => removeTier(tier.id)} className="rounded p-2 text-white/25 hover:text-rose-300"><Trash2 className="size-3.5" /></button></div></div>)}</div><div className="mx-auto mt-4 flex max-w-6xl flex-wrap items-center justify-between gap-3"><button type="button" onClick={() => setTiers((current) => [...current, { id: `tier-${Date.now()}`, label: "NEW", color: palette[current.length % palette.length], players: [] }])} className="inline-flex items-center gap-2 rounded-lg border border-dashed border-white/15 px-3 py-2 text-xs font-semibold text-white/60 hover:border-cyan-300/30 hover:text-cyan-200"><Plus className="size-3.5" /> Add tier</button><button type="button" onClick={reset} className="inline-flex items-center gap-2 rounded-lg border border-white/10 px-3 py-2 text-xs font-semibold text-white/45 hover:text-white"><RotateCcw className="size-3.5" /> Reset board</button></div><PublicCard className="mx-auto mt-8 max-w-6xl p-4 sm:p-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><p className="font-mono text-[10px] tracking-[.2em] text-cyan-300">// PLAYER POOL</p><p className="mt-1 text-sm text-white/45">{selected ? "Selected player — tap a tier row to place them." : "Drag a player into a row or tap to select."}</p></div><label className="flex items-center gap-2 rounded-lg border border-white/10 bg-black/20 px-3 py-2"><Search className="size-3.5 text-white/35" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search players" className="w-full bg-transparent text-xs text-white outline-none placeholder:text-white/25" /></label></div><div className="mt-5 flex flex-wrap gap-2">{pool.map((player) => <button key={player.id} type="button" draggable onDragStart={(event) => event.dataTransfer.setData("playerId", player.id)} onClick={() => setSelected(player.id)} className={`flex items-center gap-2 rounded-lg border px-3 py-2 text-xs font-semibold transition ${selected === player.id ? "border-cyan-300/50 bg-cyan-300/10 text-cyan-100" : "border-white/10 bg-white/[0.03] text-white/65 hover:border-white/20 hover:text-white"}`}><GripVertical className="size-3 text-white/25" /><span className="grid size-6 place-items-center rounded-md bg-white/10 font-mono text-[10px]">{player.avatar}</span>{player.name}<span className="font-mono text-[10px] text-white/30">{player.elo}</span></button>)}</div></PublicCard><p className="mx-auto mt-4 max-w-6xl font-mono text-[10px] uppercase tracking-widest text-white/30"><Pencil className="mr-2 inline size-3 text-amber-300" /> Labels and colors are yours to define.</p></main>
}
